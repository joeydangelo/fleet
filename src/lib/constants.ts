/** Branch name used for the paw sync worktree. */
export const SYNC_BRANCH = 'paw-sync';

/** Delay (ms) after killing tracked processes before removing worktrees. */
export const KILL_WAIT_MS = 1500;

/** Delay (ms) between launching consecutive agent terminals. */
export const LAUNCH_STAGGER_MS = 1500;

/** Default poll interval in seconds for watch/go commands. */
export const DEFAULT_POLL_INTERVAL = '5';
